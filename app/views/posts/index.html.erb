<div class="row">
  <div class="col-2">
    <%= render "shared/sidebar" %>
  </div>

  <div class="col-10">
    <div class="row justify-content-center">
      <div class="my-3">
        <%= form_with url: posts_path, method: :get, class: "search-bar" do %>
          <%= label_tag :query, "Search posts", class: "visually-hidden" %>
          <div class="search-bar-inner">
            <i class="fa fa-search search-bar-icon"></i>
            <%= text_field_tag :query,
                  params[:query],
                  class: "search-bar-input",
                  placeholder: "Search posts"
              %>
            <%= submit_tag "Search", name: "", class: "search-bar-btn" %>
          </div>
        <% end %>
      </div>
    </div>

    <div class="post-feed">
      <% @posts.each do |post| %>
      <%= render "shared/post", post: post %>
      <% end %>
    </div>
  </div>
</div>
