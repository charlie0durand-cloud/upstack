<div class="devise-wrapper">
  <div class="devise-card">
    <div class="devise-header">
      <h2>Account settings</h2>
      <p class="devise-subtitle">Update your profile</p>
    </div>

    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, multipart: true }) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs">
        <%= f.input :email, required: true, autofocus: true %>

        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <p class="devise-notice">Awaiting confirmation for: <%= resource.unconfirmed_email %></p>
        <% end %>

        <%= f.input :username,
                    required: false,
                    input_html: { autocomplete: "username" } %>
        <%= f.input :password,
                    hint: "Leave blank to keep your current password",
                    required: false,
                    input_html: { autocomplete: "new-password" } %>
        <%= f.input :password_confirmation,
                    required: false,
                    input_html: { autocomplete: "new-password" } %>
        <%= f.input :current_password,
                    hint: "Required to confirm your changes",
                    required: true,
                    input_html: { autocomplete: "current-password" } %>
        <div class="edit-profile-picture">
          <% if resource.profile_picture.attached? %>
            <%= image_tag resource.profile_picture, class: "current-avatar" %>
          <% end %>
          <%= f.file_field :profile_picture, class: "d-none", accept: "image/*", id: "edit-photo-upload" %>
            <label for="edit-photo-upload" class="btn-upload-photo">
              <i class="fas fa-camera me-2"></i>
              <%= resource.profile_picture.attached? ? "Changer la photo" : "Choisir une photo de profil" %>
            </label>
        </div>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "Save changes" %>
      </div>
    <% end %>
  </div>
</div>
